# 预览系统多文件支持实现方案

## 📋 项目概述

**目标：** 让预览系统支持加载所有类型的文件，包括自定义 Hook、工具函数、类型定义等。

**当前问题：** 预览系统只能加载 `src/components/` 目录下的组件文件，导致使用自定义 Hook 或工具函数的项目无法预览，出现 `ReferenceError: useTheme is not defined` 等错误。

## 🎯 实现目标

- ✅ 支持加载自定义 Hook 文件 (`src/hooks/`)
- ✅ 支持加载工具函数文件 (`src/utils/`)
- ✅ 支持加载配置文件 (`src/config/`)
- ✅ 自动处理文件间的导入依赖关系
- ✅ 保持现有组件预览功能不受影响

## 🏗️ 技术方案

### 核心思路

将所有文件编译并注册到全局作用域，类似当前处理组件的方式：

```javascript
// 组件
window.ComponentName = CompiledComponent

// Hook
window.useTheme = compiledHookFunction

// 工具函数
window.utils = {
  formatDate: compiledFunction,
  parseData: compiledFunction
}
```

### 实现步骤

#### 步骤 1：扩展文件检测逻辑

**文件位置：** [app/api/preview-code/route.ts:642-663](app/api/preview-code/route.ts#L642-L663)

**当前代码：**
```typescript
const excludedDirs = [
  'src/services/',
  'src/types/',
  'src/utils/',
  'src/lib/',
  'src/api/',
  'src/config/'
]
```

**修改为：**
```typescript
// 只排除真正无法在浏览器运行的后端代码
const excludedDirs = [
  'src/services/',
  'src/api/'
]

// 添加文件分类
const hookFiles: [string, string][] = []      // src/hooks/*.ts
const utilFiles: [string, string][] = []      // src/utils/*.ts
const configFiles: [string, string][] = []    // src/config/*.ts
const componentFiles: [string, string][] = [] // src/components/*.tsx

// 遍历所有文件并分类
for (const [filePath, code] of Object.entries(files)) {
  if (filePath.includes('/hooks/') || filePath.includes('\\hooks\\')) {
    hookFiles.push([filePath, code])
  } else if (filePath.includes('/utils/') || filePath.includes('\\utils\\')) {
    utilFiles.push([filePath, code])
  } else if (filePath.includes('/config/') || filePath.includes('\\config\\')) {
    configFiles.push([filePath, code])
  } else if (filePath.includes('/components/') || filePath.includes('\\components\\')) {
    componentFiles.push([filePath, code])
  }
}
```

#### 步骤 2：编译 Hook 文件

**插入位置：** 在组件编译循环之前（约第 736 行之前）

**实现代码：**
```typescript
// 编译所有 Hook 文件
let hookScripts = ''
console.log(`📁 Hook files found: ${hookFiles.length}`)

for (const [filePath, code] of hookFiles) {
  const hookName = filePath.split('/').pop()?.replace(/\.(ts|tsx|js|jsx)$/, '') || ''
  console.log(`🔨 Compiling hook: ${hookName}`)

  try {
    // 清理代码
    let cleanedCode = code
    // 移除 import 语句
    cleanedCode = cleanedCode.replace(/import\s+.*?from\s+['"].*?['"];?\s*/g, '')
    // 移除 export 关键字
    cleanedCode = cleanedCode.replace(/export\s+(default\s+)?(function|const|let|var)\s+/g, '$2 ')

    // 使用 Babel 编译
    const result = babel.transformSync(cleanedCode, {
      presets: [
        [presetReact, { runtime: 'classic' }],
        [presetTypescript, { isTSX: false, allExtensions: true }]
      ],
      filename: `${hookName}.ts`
    })

    if (result?.code) {
      // ���册到全局
      hookScripts += `
        // Hook: ${hookName}
        (function() {
          ${result.code}
          if (typeof ${hookName} !== 'undefined') {
            window.${hookName} = ${hookName};
            console.log('✅ Registered hook: ${hookName}');
          }
        })();
      `
      console.log(`✅ Compiled hook: ${hookName}`)
    }
  } catch (err: any) {
    console.warn(`⚠️ Failed to compile hook ${hookName}:`, err.message)
  }
}
```

#### 步骤 3：编译工具函数文件

**实现代码：**
```typescript
// 编译所有工具函数文件
let utilScripts = 'window.utils = window.utils || {};\n'
console.log(`📁 Util files found: ${utilFiles.length}`)

for (const [filePath, code] of utilFiles) {
  const fileName = filePath.split('/').pop()?.replace(/\.(ts|tsx|js|jsx)$/, '') || ''
  console.log(`🔨 Compiling util: ${fileName}`)

  try {
    // 清理代码
    let cleanedCode = code
    cleanedCode = cleanedCode.replace(/import\s+.*?from\s+['"].*?['"];?\s*/g, '')
    cleanedCode = cleanedCode.replace(/export\s+(default\s+)?(function|const|let|var)\s+/g, '$2 ')

    // 使用 Babel 编译
    const result = babel.transformSync(cleanedCode, {
      presets: [[presetTypescript, { isTSX: false, allExtensions: true }]],
      filename: `${fileName}.ts`
    })

    if (result?.code) {
      // 提取所有函数和常量，注册到 window.utils
      utilScripts += `
        // Util: ${fileName}
        (function() {
          ${result.code}
          // 尝试注册所有可能的导出
          const exports = [${fileName}];
          exports.forEach(name => {
            if (typeof eval(name) !== 'undefined') {
              window.utils[name] = eval(name);
              console.log('✅ Registered util:', name);
            }
          });
        })();
      `
      console.log(`✅ Compiled util: ${fileName}`)
    }
  } catch (err: any) {
    console.warn(`⚠️ Failed to compile util ${fileName}:`, err.message)
  }
}
```

#### 步骤 4：更新导入替换逻辑

**文件位置：** [app/api/preview-code/route.ts:815-833](app/api/preview-code/route.ts#L815-L833)

**在现有组件导入替换后添加：**
```typescript
// 替换 Hook 导入
for (const [filePath] of hookFiles) {
  const hookName = filePath.split('/').pop()?.replace(/\.(ts|tsx|js|jsx)$/, '') || ''
  if (hookName) {
    const hookPatterns = [
      // import { useTheme } from '@/hooks/useTheme'
      new RegExp(`import\\s+\\{\\s*${hookName}\\s*\\}\\s+from\\s+['"]@/hooks/${hookName}['"];?`, 'g'),
      // import { useTheme } from './hooks/useTheme'
      new RegExp(`import\\s+\\{\\s*${hookName}\\s*\\}\\s+from\\s+['"].*?/hooks/${hookName}['"];?`, 'g'),
      // import useTheme from '@/hooks/useTheme'
      new RegExp(`import\\s+${hookName}\\s+from\\s+['"]@/hooks/${hookName}['"];?`, 'g'),
    ]
    for (const pattern of hookPatterns) {
      processedAppCode = processedAppCode.replace(pattern, `const ${hookName} = window.${hookName};`)
    }
  }
}

// 替换工具函数导入
// import { formatDate, parseData } from '@/utils/format'
const utilImportPattern = /import\s+\{\s*([^}]+)\s*\}\s+from\s+['"]@\/utils\/\w+['"];?/g
processedAppCode = processedAppCode.replace(utilImportPattern, (match, imports) => {
  const importList = imports.split(',').map((i: string) => i.trim())
  return importList.map((name: string) => `const ${name} = window.utils.${name};`).join('\n')
})
```

#### 步骤 5：调整 HTML 生成顺序

**文件位置：** [app/api/preview-code/route.ts:900-950](app/api/preview-code/route.ts#L900-L950)

**修改 HTML 模板：**
```typescript
const html = `<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Preview</title>
  <script crossorigin src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
  <script crossorigin src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>
  <style>${styles}</style>
</head>
<body>
  <div id="root"></div>
  <script>
    console.log('🚀 Starting preview...');

    // 1. 先注入工具函数
    ${utilScripts}

    // 2. 再注入 Hooks
    ${hookScripts}

    // 3. 然后注入组件
    ${componentScripts}

    // 4. 最后注入主 App
    ${compiledCode}

    // 5. 渲染
    try {
      const root = document.getElementById('root');
      if (typeof App !== 'undefined') {
        ReactDOM.render(React.createElement(App), root);
        console.log('✅ App rendered successfully');
      } else {
        root.innerHTML = '<div style="padding: 20px; color: red;">Error: App component not found</div>';
      }
    } catch (err) {
      console.error('❌ Render error:', err);
      document.getElementById('root').innerHTML = '<div style="padding: 20px; color: red;">Render error: ' + err.message + '</div>';
    }
  </script>
</body>
</html>`
```

## 📝 实现进度

### 阶段 1：文件检测扩展 ✅
- [x] 修改文件排除列表
- [x] 添加文件分类逻辑（hooks、utils、config）
- [x] 测试文件检测是否正确

### 阶段 2：Hook 文件编译 ✅
- [x] 实现 Hook 文件编译逻辑
- [x] 添加全局注册代码
- [x] 测试简单 Hook（useTheme）

### 阶段 3：工具函数编译 ✅
- [x] 实现工具函数编译逻辑
- [x] 添加 window.utils 命名空间
- [x] 测试工具函数调用

### 阶段 4：导入替换 ✅
- [x] 扩展 Hook 导入替换
- [x] 扩展工具函数导入替换
- [x] 测试导入替换是否正确

### 阶段 5：HTML 生成优化 ✅
- [x] 调整脚本注入顺序
- [x] 添加错误处理和日志
- [x] 测试完整流程

### 阶段 6：测试验证 ⏳
- [ ] 创建测试项目（包含 hooks、utils、components）
- [ ] 验证预览功能正常
- [ ] 验证无运行时错误
- [ ] 性能测试

## 🧪 测试计划

### 测试项目结构
```
src/
  hooks/
    useTheme.ts       - 简单的 useState Hook
  utils/
    format.ts         - formatDate 函数
  components/
    Demo.tsx          - 使用 useTheme 和 formatDate
  App.tsx             - 主应用
```

### 测试用例

1. **基础功能测试**
   - 生成包含 hooks 的项目
   - 点击预览按钮
   - 验证无控制台错误
   - 验证组件正确渲染

2. **Hook 测试**
   - 测试 useState Hook
   - ���试 useEffect Hook
   - 测试自定义 Hook 组合

3. **工具函数测试**
   - 测试纯函数调用
   - 测试常量引用
   - 测试对象导出

4. **边界情况测试**
   - 空文件处理
   - 编译错误处理
   - 循环依赖检测

## ⚠️ 限制和注意事项

### 支持的功能
- ✅ 简单的自定义 Hook（useState、useEffect）
- ✅ 纯函数工具（formatDate、parseData）
- ✅ 常量和配置对象
- ✅ 简单的文件间依赖

### 不支持的功能
- ❌ 复杂的 npm 依赖（recharts、lucide-react 等）
- ❌ Node.js 模块（fs、path 等）
- ❌ 循环依赖
- ❌ 动态导入（import()）
- ❌ 复杂的类型推导

## 🔄 回滚方案

如果实现后出现问题：

1. **配置开关**
   ```typescript
   const ENABLE_EXTENDED_FILES = process.env.ENABLE_EXTENDED_FILES === 'true'
   ```

2. **保留原有逻辑**
   - 保持原有文件排除列表作为默认
   - 新功能作为可选特性

3. **快速回滚**
   - 恢复 excludedDirs 原始配置
   - 移除新增的编译逻辑

## 📚 相关文档

- [预览系统架构](app/api/preview-code/route.ts)
- [代码生成验证](app/api/generate-async/route.ts)
- [AI Prompt 规则](lib/ai-prompts.ts)

## 🎯 下一步行动

1. 开始实现阶段 1：文件检测扩展
2. 逐步完成各个阶段
3. 每个阶段完成后进行测试
4. 最终进行完整的集成测试

---

**文档版本：** v1.0
**创建日期：** 2026-01-22
**最后更新：** 2026-01-22
